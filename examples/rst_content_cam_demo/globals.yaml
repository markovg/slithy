# Here I define the slithy libraries
image_library:
  # the library which is used if none is specified
  path:
    - ./images
  default:
    bbp_logo: BBPLogo.png


font_library:
  path:
    - ./fonts

#add_extra_characters( u'\u201c\u201d' )

  default:

    times: times.slf
    ehrhardt: ehrhardt.slf
    body: minion.slf
    bodyi: minion_i.slf
    title: myriad_b.slf
    titlei: myriad_bi.slf
    roman: myriad.slf
    mono: courier_b.slf
    dingbats: wingding.slf
    facets: courier_b.slf



